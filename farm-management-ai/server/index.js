import express from 'express';import cors from 'cors';const app=express();app.use(cors());app.use(express.json());const PORT=process.env.PORT||5000;app.get('/health',(q,r)=>r.json({ok:true,service:'FarmOS AI'}));app.post('/api/ai/advice',(q,r)=>{const{farm,crops=[],activities=[],tasks=[]}=q.body||{},d=new Date(),a=[];const last=(t)=>activities.filter(x=>x.activity_type===t).sort((x,y)=>String(y.activity_date).localeCompare(String(x.activity_date)))[0];const w=last('watering')||last('irrigation'),s=last('pesticide');if(!w)a.push('No watering record is available. Log each irrigation event so the season history can reveal gaps.');else{const days=Math.floor((d-new Date(w.activity_date))/86400000);a.push(days>7?`Last recorded watering was ${days} days ago. Check soil moisture and rainfall before the next irrigation.`:'Recent irrigation is recorded. Check soil moisture and rainfall before watering again.')}if(s){const days=Math.floor((d-new Date(s.activity_date))/86400000);a.push(`Latest pesticide record is ${days} days old${s.reason?` and was logged for “${s.reason}”`:''}. Keep product, quantity and reason documented for traceability.`)}if(crops.length)a.push(`You are tracking ${crops.length} crop cycle(s). Link field work and harvests to each cycle for a complete plantation-to-harvest timeline.`);if(tasks.length)a.push(`You have ${tasks.filter(x=>x.status!=='done').length} open task(s). Prioritize overdue crop-stage work.`);if(!a.length)a.push('Add your farm and first crop cycle. Agro AI will turn your field records into practical, explainable guidance.');r.json({ok:true,title:`Agro AI guidance for ${farm?.name||'your farm'}`,advice:a,disclaimer:'Decision support only; not a pesticide prescription or disease diagnosis.'})});app.listen(PORT,()=>console.log(`FarmOS AI API on ${PORT}`));